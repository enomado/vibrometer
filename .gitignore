target/
recordings/

# Локальные параметры прошивки: wifi ssid/пароль + адрес хоста.
# Шаблон — firmware/config.toml.template.
firmware/config.toml

# Локальные заметки и настройки ассистента — репозиторий публичный.
.claude/
CLAUDE.md
